def solution(k, dungeons):
    # as len(dungeons) < 8, total search computations are ~ 40000
    # so use brute force to compute the solution
    max_count=0
    
    """
    어떻게 해결해야할까? 더 생각해보자.
    """
    
    return count