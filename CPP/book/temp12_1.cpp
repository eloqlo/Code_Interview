#include <iostream>
#include <array>
#include <vector>

using namespace std;

int main(){
    // vector<int> vec;

    // vec.push_back(0);
    // vec.push_back(2);
    // vec.push_back(5);
    // vec.push_back(-1);

    // for (vector<int>::iterator it = vec.begin(); it!=vec.end(); it++){
    //     cout << *it << endl;
    // }

    array<int, 5> arr;
    
    return 0;
}